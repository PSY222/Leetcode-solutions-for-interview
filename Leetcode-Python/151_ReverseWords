class Solution:
    def reverseWords(self, s: str) -> str:
        ans = ""
        s = s.strip().split()
        length = len(s)
        for i in range(length):
            ans += (s.pop() + ' ')
        return ans.strip()
        