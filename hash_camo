from itertools import combinations
import math

def solution(clothes):
    answer = len(clothes)
    cat = [] ; item = []
    org = {}
    
    for i in clothes:
            cat.append(i[1])
            item.append(i[0])
    
    for c in list(set(cat)):
        for n in range(len(clothes)):
            if c == clothes[n][1]:
                org[c] = 
    
    return org